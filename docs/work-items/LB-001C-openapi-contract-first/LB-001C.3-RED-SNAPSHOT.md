---
status: active
type: evidence
scope: backend
owner: backend-team
last-reviewed: 2026-09-24
---

# LB-001C.3 — RED snapshot

## Pruebas congeladas

| Archivo | SHA-256 al congelar RED |
|---|---|
| `SwaggerUiEnabledRuntimeTest.java` | `9982F666BB258ED5F8EB276E123AA2246B607D1FC51CBA8F47A39C964946144D` |
| `SwaggerUiDisabledRuntimeTest.java` | `9328DA078F96D731DEAD3F0803A438A7342A95BEF03DE9265B91B19557D92066` |
| `RbacSecurityFilterChainTest.java` | `FDAD560D0AAD9BC60AC60EE8FBE4C92BC877319791CF990EB34239780498F155` |

Las dos primeras iteraciones del harness se descartaron antes de congelar RED: el slice de MVC
arrastraba dependencias ajenas (`ResolverMensajeUsuarioInputPort`, `AuditEventPublisher` y
`ClientIpResolver`) y una variante no tenía `ServletContext`. Se sustituyó exclusivamente el
harness por un contexto MVC mínimo con `MockServletContext`; no se relajó ninguna aserción del
contrato.

## Ejecución RED causal

- Fecha local: `2026-09-24T20:27:12-05:00`.
- Java: 25.
- Comando: `mvn -B -ntp '-Dtest=SwaggerUiEnabledRuntimeTest,SwaggerUiDisabledRuntimeTest,RbacSecurityFilterChainTest' test`.
- Resultado: `BUILD FAILURE`, exit code `1`.
- Resumen: 35 pruebas, 4 fallos, 0 errores, 0 omitidas.

Fallos esperados antes de implementar:

1. Los GET públicos de documentación devolvían `401` en la cadena de seguridad.
2. `/openapi/openapi-golden-path.yaml` devolvía `404`.
3. `/swagger-ui/` devolvía `404` en lugar de redirigir al índice.
4. El inicializador de Swagger UI devolvía `404`.

La prueba del estado deshabilitado pasó y `/v3/api-docs` permaneció ausente. La señal RED es
contractual: faltaban el recurso publicado, el UI y su excepción GET de seguridad; no hubo fallo
de compilación ni de infraestructura.
