---
status: active
type: test-evidence
scope: backend
owner: backend-team
last-reviewed: 2026-09-24
---

# LB-001C.1A — RED snapshot de Contract Hardening

## Resultado

**RED causal confirmado.** El YAML de C.1 se mantuvo intacto durante esta captura.

- Base Git: `fa9aa901c73e55ae31071f4e74cfb2245189243a`, rama `sergio`, más worktree previo
  preservado.
- Fecha: 2026-09-24 16:24:34 -05:00.
- Comando: `./mvnw.cmd '-Dtest=OpenApiGoldenPathConformanceTest' test` con JDK 25.
- Exit code: `1` (`BUILD FAILURE` por assertions contractuales).
- Resultado: 7 tests, 3 failures, 0 errors, 0 skipped.
- SHA-256 RED `OpenApiGoldenPathConformanceTest.java`:
  `c89c143192b446b49aadddc78af8ed9fd300549cca0286bac99b57b38ba7852e`.
- SHA-256 YAML C.1 evaluado: `SUPERSEDED_BY_LB-001C.1A`; el SHA canónico vigente se registra
  en VALIDATION y el archivo `.sha256`. La salida RED conserva los valores semánticos observados.

## Fallos causales observados

1. `canonicalServerIsRelativeAndPortable`: esperaba `/`; obtuvo
   `http://localhost:8080`.
2. `localSessionWireAndUtcInstantsUseDifferentOpenApiSemantics`: esperaba ausencia de format;
   obtuvo `date-time` en `LocalSessionDateTime`.
3. `criticalSchemasDeclareFrozenTypesUuidFormatsAndConstraints`: esperaba `format: uuid` en
   `RealtimeEventResponse.correlationId`; obtuvo ausencia de format. El campo transporta el UUID
   efectivo de la petición según el contrato de correlación aprobado.

Los gates adicionales de properties/required/additionalProperties, Bearer JWT, parameter
CorrelationId en 8/8 operaciones y header de success en 8/8 no fallaron con el YAML C.1.

## Corrección trazada del tester posterior al RED

La primera ejecución GREEN alcanzó una assertion que los tres fallos RED anteriores habían
enmascarado: Swagger Parser 3.1 inlinea `items` y no conserva `items.$ref` en ese modelo cargado.
Se sustituyó la comprobación redundante del `$ref` por `items != null`; `minItems=1` permanece y
la integridad de `$ref` sigue cubierta por OAS-02/`OpenApiGoldenPathValidationTest`. No cambió
ninguna expectativa contractual ni el YAML para acomodar esta limitación del parser. El SHA del
test GREEN se registrará en VALIDATION.

## Intentos no contabilizados como RED

- Un intento terminó antes de cargar el proyecto por bloqueo de red a Maven Central
  (`Permission denied: getsockopt`).
- Dos ejecuciones preliminares detectaron que Swagger Models 3.1 representa `type` mediante
  `Schema.types` y `additionalProperties: false` mediante un boolean schema. Se corrigió
  exclusivamente la lectura del test antes de congelar este snapshot; no se cambió contrato.

## Restauración final OAS-07D — NO_RED_OBSERVED

La corrida baseline del 2026-09-24 16:48:17 -05:00, antes de agregar el gate provider DTO,
terminó GREEN: 9 tests OpenAPI, 0 fallos, 0 errores y 0 skips. Los DTO Java y el YAML ya estaban
alineados; la brecha era la ausencia del regression gate, no un mismatch vigente. No se fabricó
RED ni se modificó producción. El primer intento no cargó el proyecto por bloqueo de red del
sandbox a Maven Central (`Permission denied: getsockopt`) y no se contabiliza como resultado
contractual.
