---
status: complete
type: validation
scope: backend
owner: backend-team
last-reviewed: 2026-09-24
---

# VALIDATION — LB-001C.3

## Dictamen

**PASS.** Swagger UI runtime consume exclusivamente el OpenAPI canónico, funciona sin CDN ni
generador code-first, queda deshabilitada por defecto y no abre la API de negocio. El YAML fuente
no cambió. No se ejecutó ni autorizó una fase posterior.

## Ambiente y alcance

- Windows / PowerShell, Java 25, Maven 3.9.11, Spring Boot 4.0.6.
- Rama observada: `sergio`; base de entrada:
  `fa9aa901c73e55ae31071f4e74cfb2245189243a`.
- Worktree previo del usuario preservado; sin commit ni push.
- Persistencia, DB, frontend, Keycloak real, JPA, Redis, RabbitMQ y serverless: **NO APLICA**;
  ninguna de esas superficies cambió.

## Evidencia REQUIREMENT → RED → GREEN

| Gate | Fecha local / comando | Resultado |
|---|---|---|
| Baseline | 2026-09-24 20:08 -05:00 — `mvn -B -ntp verify` | PASS; 987 tests, 0F/0E/0S; OpenAPI y ArchUnit verdes; cobertura dentro de gates |
| RED congelado | 2026-09-24 20:27 -05:00 — `mvn -B -ntp '-Dtest=SwaggerUiEnabledRuntimeTest,SwaggerUiDisabledRuntimeTest,RbacSecurityFilterChainTest' test` | FAIL esperado; 35 tests, 4 fallos, 0 errores, 0 skips. Causas: docs 401 y UI/YAML/initializer 404 |
| GREEN dirigido | 2026-09-24 20:33 -05:00 — mismo comando dirigido | PASS; 35/35, 0F/0E/0S |
| Verify final | 2026-09-24 20:48 -05:00 — `mvn -B -ntp verify` | **BUILD SUCCESS**; 993 tests, 0F/0E/0S; paquete generado |
| ArchUnit final | incluido en `verify` | 67/67, 0F/0E/0S |
| JaCoCo final | incluido en `verify` | LINE 86.71 % (`6943/8007`), BRANCH 70.98 % (`1414/1992`); gates 80/70 cumplidos |
| Diff hygiene | `git diff --check` | PASS; solo warning preexistente de conversión LF/CRLF en `.gitignore` |

Los hashes de los tests congelados tras el RED siguieron iguales al cierre:

| Test | SHA-256 |
|---|---|
| `SwaggerUiEnabledRuntimeTest.java` | `9982F666BB258ED5F8EB276E123AA2246B607D1FC51CBA8F47A39C964946144D` |
| `SwaggerUiDisabledRuntimeTest.java` | `9328DA078F96D731DEAD3F0803A438A7342A95BEF03DE9265B91B19557D92066` |
| `RbacSecurityFilterChainTest.java` | `FDAD560D0AAD9BC60AC60EE8FBE4C92BC877319791CF990EB34239780498F155` |

La primera implementación GREEN dejó un único 404 porque un resource handler exacto no producía
ruta relativa. Se cambió solo el patrón productivo a `/openapi/**`; los tests RED no se tocaron.
Después, la ejecución HTTP real mostró MIME genérico y se añadió un mapping `yaml →
application/yaml`; el `verify` final corresponde a ese estado definitivo.

## Fuente única y empaquetado

| Evidencia | Resultado |
|---|---|
| SHA canónico antes y después | `72A3097BBAE2A296ED6690BF89C239F56697764F25100B930D941A699736DA54` |
| SHA copia `target/classes/runtime-docs/openapi/...` | mismo SHA — MATCH byte-for-byte |
| JAR | 1 YAML runtime y 5 assets Swagger seleccionados dentro de `BOOT-INF/classes` |
| Generadores/librerías Swagger runtime | 0 JARs `springdoc`, `swagger-core` o `swagger-ui` bajo `BOOT-INF/lib` |
| `/v3/api-docs` | ningún mapping/generador; prueba MVC 404. En la cadena real anónima cae antes en 401 por `anyRequest().authenticated()` |

El WebJar `org.webjars:swagger-ui:5.32.15` solo es insumo de build. Maven desempaqueta CSS,
bundle, standalone preset y favicons; el navegador no descarga recursos externos.

## HTTP real del JAR

Se levantó el JAR final con perfil `local` en `127.0.0.1:18080`. El puerto 8080 mostró una
colisión transitoria del host; se escogió un puerto libre sin cambiar configuración versionada.
Para satisfacer el arranque del proveedor JWT se usó un emisor OIDC efímero localhost con clave
pública ficticia y valores dummy no secretos. No se conectó a DB ni a un IdP real. Al terminar se
detuvieron backend y mock; puertos 18080/8081 sin listeners.

| GET | Estado | Content-Type / observación |
|---|---:|---|
| `/swagger-ui/` | 200 tras redirect | `text/html` |
| `/swagger-ui/index.html` | 200 | `text/html` |
| CSS, bundle, standalone preset, initializer | 200 | `text/css` / `text/javascript`; todos localhost |
| `/openapi/openapi-golden-path.yaml` | 200 | `application/yaml`; SHA runtime = SHA canónico |
| `/api/v1/docentes` sin Bearer | 401 | API business continúa protegida |

## Validación visual en navegador

PASS con el navegador embebido sobre el JAR final:

- título `AsistenciasUCO — Golden Path API 1.0.0 OAS 3.1` y fuente visible
  `/openapi/openapi-golden-path.yaml`;
- 9 operaciones: GET 5, POST 2, PATCH 1, PUT 1, DELETE 0;
- `actualizarSesion` PATCH visible y expandible; parámetros, `application/json`, request body y
  controles Cancel/Execute presentes por `tryItOutEnabled=true`;
- `actualizarSesionLegacy` PUT visible y marcado deprecated (`deprecatedCount=1`);
- `operationId`, tags, schemas, candados y botón Authorize visibles;
- scripts y CSS observados únicamente bajo `http://127.0.0.1:18080/swagger-ui/**`;
- consola del navegador: 0 warnings y 0 errores.

Swagger UI muestra en el documento un aviso no bloqueante: el `jsonSchemaDialect` canónico difiere
del dialecto base que esa versión soporta como default. Las 9 operaciones y schemas se renderizan;
no hay error de consola. El YAML es válido y no se alteró para silenciar una limitación del visor.
SSE se presenta como contrato, pero no se promete ejecución interactiva durable desde Swagger UI.

## Seguridad y configuración

- Property: `asistencias.swagger-ui.enabled`.
- Default/producción: `false`, sobrescribible por `ASISTENCIAS_SWAGGER_UI_ENABLED`.
- Perfiles `local` y `dev`: `true`.
- Property `false`: UI, assets y YAML devuelven 404 en el contexto MVC.
- Solo GET de `/swagger-ui`, `/swagger-ui/**` y el YAML canónico es `permitAll`.
- `/api/v1/**`, JWT, RBAC y ownership no se relajaron.
- `persistAuthorization=false`; no se añadieron secretos, CSP laxa, CORS global ni CDN.

## Limitaciones y gates no ejecutados

- `mvn -Pintegration verify`: **NO APLICA** a C.3 porque no cambió persistencia/integración DB;
  el estado histórico TD-043 fuera del Golden Path no se reinterpreta.
- CI remoto/Rulesets/Sonar: no ejecutados ni inferidos desde Maven local; MV-004 permanece
  pendiente por su propio alcance.
- No se ejecutó una petición mutante real desde Try It Out para evitar efectos de negocio. Su
  disponibilidad y construcción de parámetros/body sí se validaron visualmente.

## Resultado DoD aplicable

Alcance/contrato, RED→GREEN, build, arquitectura, cobertura, seguridad, operación y documentación:
PASS. Persistencia/DB y CI remoto: NO APLICA o no ejecutado con límite explícito. No hay conflicto
contractual, deuda nueva bloqueante ni evidencia de secretos.
