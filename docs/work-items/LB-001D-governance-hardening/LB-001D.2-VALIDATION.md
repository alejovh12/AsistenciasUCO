---
status: active
type: work-item
scope: backend
owner: backend-team
last-reviewed: 2026-09-26
---

# LB-001D.2 — VALIDATION

Fecha: 2026-09-26. Base: `4dbbc688505f93aa5a9b6149b43eff9e3356f449` (== `origin/sergio`, verificado al inicio y al cierre). Java 25 (`openjdk 25`), Spring Boot 4.0.6, Windows 11.

| # | Comando | Exit | Resultado | Notas |
|---|---|---|---|---|
| 1 | `git status -sb`, `git fetch origin`, `rev-parse HEAD/origin/sergio` | 0 | PASS | branch `sergio`, limpio, sin avance remoto |
| 2 | `.\mvnw.cmd -q -B -ntp test -Dtest=<4 clases RED>` (pre-implementación) | 1 | **RED**: 23 tests, 6 failures | causal, ver [RED-SNAPSHOT](LB-001D.2-RED-SNAPSHOT.md) |
| 3 | mismo filtro + `AzureEventGridWebhookControllerTest`, `ProcesarEventoAzureUseCaseImplTest`, `RbacSecurityFilterChainTest`, `SecurityConfigTest` | 0 | PASS 72/72 | GREEN dirigido |
| 4 | `.\mvnw.cmd -B -ntp verify` | 0 | **BUILD SUCCESS**, 1028 tests, 0 failures, 0 errors, 0 skipped | baseline BASE_SHA: 1009 (medido con `mvnw test` en worktree limpio en BASE_SHA). Δ = +19 = +20 nuevos − 1 (E2E Azure real removido de Surefire) |
| 5 | JaCoCo (`target/site/jacoco/jacoco.xml`, misma corrida) | — | gates cumplidos ("All coverage checks have been met") | LINE 86.28 % (7053/8175), BRANCH 70.37 % (1482/2106) — calculado de los contadores del XML |
| 6 | ArchUnit | — | PASS | incluido en (4); 0 fallos en `architecture/*` |
| 7 | `.\mvnw.cmd -B -ntp -Pazure-integration verify` **sin** variables de endpoint | 1 | FAIL explícito esperado | "Ambiente Azure no disponible: defina AZURE_KEYVAULT_ENDPOINT…"; solo corrió `AzureCloudIntegrationIT`; Surefire "Tests are skipped", JaCoCo skipped por perfil. Demuestra fallo claro, no PASS falso |
| 8 | `.\mvnw.cmd -B -ntp -Pazure-integration verify` con identidad `az login` activa y endpoints tomados de `application.yml` | 0 | **PASS** 1/1 (`AzureCloudIntegrationIT`, 8.5 s) | solo lectura; valores no impresos |
| 9 | `help:effective-pom` con/sin `-Pintegration` | 0 | PASS estructural | `integration`: include `**/*IT.java` + exclude `**/*CloudIntegrationIT.java`; `azure-integration`: solo `**/*CloudIntegrationIT.java` |
| 10 | `.\mvnw.cmd -Pintegration verify` (SQL Server) | — | **NOT_RUN_BY_ENVIRONMENT** | sin `SPRING_DATASOURCE_*` ni DB controlada; TD-043 ya documenta ese perfil como rojo. No se declara PASS |
| 11 | `git diff --check` | 0 | PASS | |
| 12 | Auditoría de secretos en archivos modificados | — | REAL_SECRET_FOUND = 0 | el único match del patrón es el nombre de variable `dbPassword` (sin valor) en la IT |

## Azure `verify` normal no ejecuta Azure real (evidencia por configuración/reportes)

- `target/surefire-reports` de la corrida (4) no contiene `AzureCloudIntegration*`; no existe `target/failsafe-reports` en esa corrida (sin perfil).
- El log de `verify` no menciona `AzureCloudIntegration`.
- `CloudIntegrationClassificationTest` (parte de `verify`) falla si reaparece una prueba de lectura real bajo un nombre distinto de `*CloudIntegrationIT`. Señales: wiring `AzureKeyVaultAdapterConfiguration` + `ApplicationContextRunner` + `.getSecret(` y una señal operacional (`AZURE_KEYVAULT_ENDPOINT`, `AZURE_APPCONFIG_ENDPOINT` o `.vault.azure.net`). Auto-prueba: el contenido actual de `AzureCloudIntegrationIT` es DETECTADO como Cloud; `VaultAdapterConfigurationTest` (wiring/fail-fast) NO se marca. Límite: un test Cloud que no use esas señales no sería detectado.
- No hay `@Disabled`, `assume*` ni `catch/return` en la solución.

## Contratos

| Contrato | SHA-256 | Estado |
|---|---|---|
| OpenAPI `docs/contracts/openapi/openapi-golden-path.yaml` | `72a3097bbae2a296ed6690bf89c239f56697764f25100b930d941a699736da54` | UNCHANGED (MATCH) |
| Backend Golden Path `BACKEND_GOLDEN_PATH_CONTRACT.md` | `9b4830b468b58b49793658454a7b34deec20d2d9681943aa8f1e3942b15d8c62` | UNCHANGED (MATCH) |
| DB Contract Snapshot `DB_BASELINE_CONTRACT.md` | `45e48c5a0ab321d0c8cbffb55ee224e3b6fd29febc39a62ca723b2b209945aec` | UNCHANGED (MATCH) |

OpenAPI: 9 operaciones; PATCH canónico, PUT deprecated (sin cambios; los gates OpenAPI/Swagger corren en `verify` y pasan).

## Limitaciones

- Los tests del filtro/controller/use case/adapters usan mocks o clientes controlados: **no certifican Azure real**.
- El PASS de `AzureCloudIntegrationIT` demuestra lecturas reales de `SecretVaultPort`, `ParameterCatalogPort` y `MessageCatalogPort` en el ambiente dev; **no** demuestra Event Grid real, invalidación/refresh end-to-end ni observabilidad. **MV-003 permanece PENDIENTE.**
- Event Grid real: NOT_RUN. El handshake se probó con evento simulado y credencial de prueba.
- SQL Server integration: NOT_RUN_BY_ENVIRONMENT.
