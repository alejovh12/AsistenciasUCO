---
status: active
type: work-item
scope: backend
owner: backend-team
last-reviewed: 2026-09-26
---

# LB-001D.2 — TEST PLAN

Valores de credencial: literales TEST_ONLY (`unit-test-webhook-token`, `wrong-test-only-token`). El literal `dev-azure-event-token-change-in-prod` aparece solo en `AzureWebhookUnconfiguredSecurityChainTest` como regresión del default retirado. RED y snapshot: [RED-SNAPSHOT](LB-001D.2-RED-SNAPSHOT.md).

## Matriz de comportamiento

| ID | Requirement | Given | When | Observable | Expected | Implementación equivocada detectada | Nivel |
|---|---|---|---|---|---|---|---|
| SEC-AZ-001 | TD-051 fail-closed | credencial nula/vacía/en blanco, o propiedad ausente | POST al webhook (con header, con default histórico, con query, sin nada) | status + chain no invocada + InputPort sin llamadas | 401 | default funcional restaurado; "permitir en local"; filtro desactivado si no hay token | Unit filtro + Component (cadena real `SecurityConfig`) |
| SEC-AZ-002 | TD-052 header only | credencial correcta solo en `?token`, `?access_token`, `?key`, `?aeg-sas-token` | POST sin header | 401 y sin procesamiento | 401 | fallback a `getParameter`; query aceptado con CSRF exento | Unit + Component |
| SEC-AZ-002b | idem | header incorrecto + query correcto | POST | 401 | 401 | query como respaldo del header inválido | Unit |
| SEC-AZ-003 | header inválido | header ausente / vacío / en blanco / distinto / prefijo / sufijo | POST | 401 | 401 | comparación por prefijo/longitud; header vacío aceptado | Unit + Component |
| SEC-AZ-004 | header válido | header = credencial (con espacios laterales tolerados) | POST | la cadena continúa | 200 / chain | rechazo de credencial válida | Unit + Component |
| SEC-AZ-005 | handshake preservado | header válido + `SubscriptionValidationEvent` | POST | `validationResponse` | 200 + eco | handshake roto; handshake sin credencial | Component |
| SEC-AZ-005b | idem | sin credencial + handshake | POST | 401 | 401 | handshake abierto | Component |
| SEC-AZ-006 | procesamiento preservado | header válido + evento regular | POST | `procesarEvento(tipo, subject, data)` y `datos.processed=1` | 200 | evento no delegado; CSRF bloqueando el webhook | Component (+ `AzureEventGridWebhookControllerTest`, `ProcesarEventoAzureUseCaseImplTest` existentes) |
| SEC-AZ-007 | API de negocio sin cambios | GET/POST `/api/v1/sesiones` sin JWT (aun con header del webhook) | request | GET 401; POST 403 (CSRF, AS-IS) | igual que BASE | header del webhook abre rutas de negocio; `/api/v1/**` público | Component + `RbacSecurityFilterChainTest` y `SecurityConfigTest` existentes |
| SEC-AZ-008 | path exacto | `/otro/api/v1/internal/azure-events` | POST | filtro no intercepta; ruta no exenta de CSRF/auth | chain continúa (filtro) / 4xx (cadena) | `endsWith` amplio | Unit + Component |
| SEC-AZ-009 | context path | `/app` + ruta | POST sin credencial | 401 | 401 | romper context path | Unit |
| TD053-001 | clasificación | código de test que lee Key Vault real bajo `*Test` | análisis de fuentes de test | ninguna fuente fuera de `*CloudIntegrationIT` | lista vacía | regresar a `*Test` | Architecture/build |
| TD053-002 | perfiles | POM efectivo | `help:effective-pom` | `integration` excluye `*CloudIntegrationIT`; `azure-integration` solo la incluye | estructura | mezclar SQL IT con Azure | Estructural (ver VALIDATION) |
| CLOUD-001 | lectura real (solo lectura) | ambiente Azure autorizado | `-Pazure-integration verify` | Ports devuelven secreto no vacío/parámetro/mensaje | PASS o FAIL explícito | mock = cloud PASS; skip silencioso | Cloud Integration |

## Notas

- SEC-AZ-007 fija el AS-IS: POST sin Bearer ni CSRF devuelve 403 (CSRF), GET 401; no se reescribe la política.
- Cobertura existente conservada: `AzureEventGridAuthFilterTest` (3 casos originales incluidos), `AzureEventGridWebhookControllerTest`, `ProcesarEventoAzureUseCaseImplTest`, tests de adapters Azure.
- Ningún test usa skip/assumptions para ocultar clasificación.
