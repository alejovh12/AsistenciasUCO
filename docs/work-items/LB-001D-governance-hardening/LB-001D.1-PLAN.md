---
status: active
type: work-item
scope: backend
owner: backend-team
last-reviewed: 2026-09-26
---

# PLAN — LB-001D.1: Governance + Agents + Skills + Azure Documentation Sync

## Identidad y objetivo

- Fecha / rol / base Git: 2026-09-26 / ejecución asistida con separación conceptual de roles / `sergio@f121c34e943db8f33579421506becb0e48747414`.
- Objetivo: sincronizar gobernanza, agentes, skills y documentación de arquitectura, testing y Azure con el código AS-IS antes de LB-001D.2 y LB-002.
- Change class: `DOCUMENTATION_ONLY`.
- PRIMARY_VARIABLE: consistencia documental y operativa del gobierno del backend.
- Skills: `uco-arquitectura`, `uco-baseline`, `uco-catalogos`, `uco-contratos`, `uco-observabilidad`, `uco-persistencia`, `uco-realtime`, `uco-seguridad`, `uco-testing`; se crea `uco-azure` como resultado.

## Precondición Git

| Check | Resultado |
|---|---|
| Branch | `sergio` |
| HEAD | `f121c34e943db8f33579421506becb0e48747414` |
| `origin/sergio` después de `git fetch origin` | `f121c34e943db8f33579421506becb0e48747414` |
| Worktree inicial | limpio |

## AS-IS y evidencia

| Hecho | Evidencia | Límite |
|---|---|---|
| Vault Azure usa `SecretVaultPort`, `AzureKeyVaultAdapter`, `DefaultAzureCredential` y Caffeine 50/5 min | adapter y Composition Root en `src/main/java/**/vault/azure` | inspección estática; no certifica Azure desplegado |
| Parámetros Azure usan `ParameterCatalogPort`, App Configuration y Caffeine 1000/10 min | `AzureAppConfigParameterCatalogAdapter` | inspección estática |
| Mensajes Azure usan claves `messages:user:` label `es` y `messages:technical:` sin label; dos caches Caffeine 2000/30 min | `AzureAppConfigMessageCatalogAdapter` | inspección estática |
| Event Grid entra por `POST /api/v1/internal/azure-events`, llama `ProcesarEventoAzureInputPort` y luego `CatalogInvalidationPort` | controller, use case, Composition Root y tests | no implica validación de webhook real |
| Algunos cambios de App Configuration publican `MESSAGE_UPDATED` o `PARAMETER_UPDATED` | `ProcesarEventoAzureUseCaseImpl` | su visibilidad cliente requiere decisión DR-AZ-001 |
| El webhook tiene default funcional inseguro y admite credencial por query | `SecurityConfig`, `AzureEventGridAuthFilter` | el valor no se registra |
| `AzureCloudIntegrationE2ETest` usa Azure real y su nombre `*Test` lo incluye en la suite normal | test y convención Maven Surefire/Failsafe | corrección técnica fuera de LB-001D.1 |

## TARGET

- Documentación normativa Azure AS-IS, sin valores secretos ni afirmaciones de despliegue.
- Gobierno de testing orientado a comportamiento, niveles y proveedores reales.
- Agentes, skills y plantillas alineados con testing, Azure, seguridad y pre-JPA.
- Deudas y decisión realtime registradas una sola vez y con target explícito.
- LB-001D.1 documentado como cierre documental; LB-001D.2 queda `PLANNED`; LB-002 queda bloqueado por LB-001D.2.

## Alcance de rutas

### ALLOWED_PATHS

- `AGENTS.md`
- `.claude/agents/**`
- `.claude/skills/**`
- `.claude/templates/**`
- `docs/**`

### FORBIDDEN_PATHS

- `src/main/**`
- `src/test/**`
- `pom.xml`
- `src/main/resources/application*.yml`
- `.github/**`
- `infra/**`
- frontend, DB, Keycloak y recursos Azure externos
- `docs/contracts/openapi/**` y contratos Golden Path

Lo no listado como permitido no se modifica. No commit, no push.

## Contratos, providers y consumidores

- CONTRACTS: no se modifican OpenAPI, contrato Golden Path, DB snapshot ni contrato realtime.
- PROVIDERS: Azure Key Vault, Azure App Configuration, Azure Event Grid, SQL Server y providers locales existentes; solo documentación.
- EXTERNAL_ENVIRONMENT: Azure real no es requerido para esta fase documental; MV-003 permanece pendiente.
- CONSUMERS: backend Application/Infrastructure, equipos de operaciones y seguridad, testers y la futura fase LB-002. El frontend no cambia.

## Seguridad y observabilidad

- SECURITY_IMPACT: documental; registra hallazgos del webhook sin revelar valores.
- OBSERVABILITY_IMPACT: documental; define evidencia sanitizada de tipo de evento, clasificación de fallos e invalidación, sin inventar métricas.

## Test plan

- TEST_LEVEL_REQUIRED: validación documental.
- RED: `NOT_APPLICABLE_DOCUMENTATION_ONLY`.
- Reason: no runtime behavior change.
- `git diff --check`, alcance del diff, referencias internas y búsqueda sanitizada de patrones sensibles.
- Maven/Java: no obligatorio porque no cambia Java; no declarar tests Java afectados ni ejecutados.

## Riesgos y rollback

- Riesgo: documentar intención en vez de AS-IS. Mitigación: citar símbolos y distinguir TARGET/deuda.
- Riesgo: duplicar reglas. Mitigación: AGENTS corto, skill operativa, norma detallada, work item con evidencia.
- Riesgo: exponer secretos durante revisión. Mitigación: registrar solo tipo/archivo; nunca valor.
- ROLLBACK: revertir únicamente los archivos documentales listados por este work item; no hay migración de datos ni cambio de runtime.

## Stop conditions

- `origin/sergio` distinto del SHA autorizado.
- Worktree inicial contaminado.
- `CONTRACT_CONFLICT` entre fuentes activas no resoluble como corrección AS-IS.
- Valor secreto real detectado en archivos nuevos/modificados.
- Cambio fuera de `AGENTS.md`, `.claude/**` o `docs/**`.
- Modificación accidental de OpenAPI, contratos Golden Path, producción, tests, POM o configuración runtime.

## Deuda y validación manual

- Nuevas entradas: seguridad del webhook, clasificación de integración Azure y decisión Azure→realtime, sin duplicar IDs existentes.
- MV-003 se amplía para Key Vault, App Configuration, Event Grid, invalidación y refresco de caché; permanece pendiente.

## Definition of Ready

`READY` para `DOCUMENTATION_ONLY`: objetivo, autoridad, variable, providers, rutas, consumidores, riesgos, rollback, test documental y stop conditions están definidos; el worktree y el SHA cumplen la precondición. No autoriza implementación de LB-001D.2 ni LB-002.
