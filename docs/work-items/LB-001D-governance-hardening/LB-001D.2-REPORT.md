---
status: active
type: work-item
scope: backend
owner: backend-team
last-reviewed: 2026-09-26
---

# LB-001D.2 — AZURE SECURITY + CLOUD TEST ISOLATION REPORT

**BASE SHA:** `4dbbc688505f93aa5a9b6149b43eff9e3356f449` · **JAVA:** 25 · **SPRING BOOT:** 4.0.6 · **Estado:** PASS, pendiente de revisión humana (sin commit ni push).

## Archivos modificados

Producción: `AzureEventGridAuthFilter.java`, `SecurityConfig.java`. Config: `application.yml`, `pom.xml`. Tests: `AzureEventGridAuthFilterTest` (reescrito, casos originales conservados), `AzureWebhookSecurityChainTest`, `AzureWebhookUnconfiguredSecurityChainTest`, `CloudIntegrationClassificationTest` (nuevos), `AzureCloudIntegrationE2ETest` → `AzureCloudIntegrationIT` (rename + endpoints por entorno + solo lectura). Docs: work item LB-001D.2 (6 archivos), `TECHNICAL_DEBT`, `LINEA_BASE`, `MANUAL_VALIDATION_LEDGER`, `azure-runtime-integration`, `runtime-security-provider-architecture`, `TESTING_STANDARD`, `VALIDATION_RUNBOOK`. Skills/agentes sin cambios (sus instrucciones AS-IS no cambiaron).

## Security

| Ítem | Resultado |
|---|---|
| WEBHOOK PROPERTY | `app.security.azure-events.webhook-token` ← `AZURE_EVENTGRID_WEBHOOK_TOKEN` (vacío por defecto) |
| FUNCTIONAL DEFAULT REMOVED | YES |
| EMPTY CONFIG FAIL CLOSED | PASS (nulo/vacío/blanco/propiedad ausente → 401) |
| AUTH HEADER | `aeg-sas-token` |
| QUERY TOKEN REMOVED | YES (`token`, `access_token`, `key`, `aeg-sas-token` por query → 401) |
| MISSING / EMPTY / WRONG HEADER | PASS / PASS / PASS (401) |
| QUERY-ONLY | PASS (401) |
| CORRECT HEADER | PASS (continúa la cadena) |
| CONSTANT-TIME COMPARISON | YES: SHA-256 + `MessageDigest.isEqual` (JDK; iguala longitudes; sin dependencias) |
| PATH MATCHING | exacto (URI − context path); context path soportado; rutas que solo terminan igual ya no se interceptan ni se eximen de CSRF |
| HANDSHAKE | PASS (con credencial); sin credencial → 401 |
| NORMAL EVENT | PASS |
| SECRET EXPOSURE | 0 |

## Test isolation

| Ítem | Resultado |
|---|---|
| OLD CLOUD TEST | `AzureCloudIntegrationE2ETest` (`*Test`, Surefire, endpoints hardcodeados) |
| NEW CLASSIFICATION | `AzureCloudIntegrationIT` (Cloud Integration, Failsafe, solo lectura) |
| NORMAL VERIFY RUNS AZURE REAL | NO |
| AZURE PROFILE | `.\mvnw.cmd -Pazure-integration verify` (solo `*CloudIntegrationIT`) |
| SQL INTEGRATION PROFILE | `.\mvnw.cmd -Pintegration verify` — excluye `*CloudIntegrationIT`; estructura verificada |
| SKIP-BASED ISOLATION | NO |

## RED / GREEN

- **RED:** 4 clases, 23 tests, **6 failures**, causal = YES (TD-051 ×2, TD-052 ×2, path matching ×1, TD-053 ×1). Ver [RED-SNAPSHOT](LB-001D.2-RED-SNAPSHOT.md).
- **Dirigido:** 72/72 PASS. **MAVEN VERIFY:** PASS, **1028/1028** (baseline 1009), 0 failures/errors/skipped. **ARCHUNIT:** PASS. **JACOCO:** LINE 86.28 %, BRANCH 70.37 % (gates 80/70 cumplidos; el branch bajó desde 70.98 % por las ramas nuevas del filtro, margen 0.37 pp).

## Integration

| Ítem | Resultado |
|---|---|
| SQL INTEGRATION | NOT_RUN_BY_ENVIRONMENT |
| AZURE CLOUD INTEGRATION | PASS (`az login` activo, solo lectura); sin variables de endpoint falla explícitamente |
| AZURE REAL READS | SecretVaultPort = PASS · ParameterCatalogPort = PASS · MessageCatalogPort = PASS |
| EVENT GRID REAL | NOT_RUN |
| MV-003 | PENDIENTE (evidencia parcial anotada en el ledger; no cubre Event Grid, invalidación, observabilidad) |

## Deuda

TD-051 RESOLVED · TD-052 RESOLVED · TD-053 RESOLVED · **TD-054 OPEN / DECISION_REQUIRED** · **DR-AZ-001 DECISION_REQUIRED** (sin tocar `ProcesarEventoAzureUseCaseImpl` ni SSE).

## Contratos

OpenAPI `72a3097b…da54`, Backend `9b4830b4…8c62`, DB `45e48c5a…9aec`: UNCHANGED = YES. Operaciones OpenAPI: 9. PATCH: CANONICAL. PUT: LEGACY_DEPRECATED.

## Scope

Producción: `AzureEventGridAuthFilter`, `SecurityConfig`. Tests: ver arriba. POM: YES (perfil `azure-integration` y exclusión en `integration`). Runtime config: `application.yml` (`webhook-token` vacío por defecto). OpenAPI/Frontend/DB/Keycloak: NO. JPA: NO. Secretos: 0. `git diff --check`: PASS. COMMIT: NO. PUSH: NO. Nota: el `git mv` dejó el rename en el índice (staged); nada más está staged.

## Acción operativa requerida antes de desplegar

Definir `AZURE_EVENTGRID_WEBHOOK_TOKEN` y configurar el header `aeg-sas-token` en la suscripción Event Grid; despliegues que dependían del default o del query token recibirán 401 (intencional).

## FINAL

**LB-001D.2: PASS** · **LB-001D: READY_TO_CLOSE** (sujeto a revisión humana; MV-003 y TD-054 permanecen abiertos por diseño) · **READY FOR JPA LB-002: YES tras la revisión humana** (TD-043 documentado como deuda fuera de alcance).

STOP — a la espera de revisión humana.
